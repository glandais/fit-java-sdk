// Hand-written multiplatform port of BufferedRecordMesg.java — keep in sync (see overrides/hashes.json).
// Requires the generated RecordMesg to be an `open class` with a no-arg constructor.
package io.github.glandais.fit

class BufferedRecordMesg : RecordMesg()
